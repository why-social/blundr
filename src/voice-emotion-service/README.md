# voice-emotion-service
This is a top-level directory containing the components of the Voice Emotion 
Recognition Service (VER).

See respective directories for more documentation.

## Structure

`./common` - a library of common functionality (e.g. data processing) between 
the training and the runtime (inference) scripts.

`./infer` - the runtime VER service that runs inference

`./train` - the training script

