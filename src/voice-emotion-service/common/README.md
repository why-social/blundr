# ve-common

This library contains common functionality between train- and inferrence for the VE model.

## Structure

`./src/common/config` - configuration dataclasses

`./src/common/model` - the definition of the torch model(s)

`./src/common/utils` - common utilities (audio processing and transformations)

